To connect to your new Dokos installation, use the password defined during setup and "administrator" as login.

If you're stuck on a session expired page, clear the cache and cookies or connect in a private browser window.
